<% if object.errors.any? %>
	<div class="alert">
		This page has <%= pluralize(object.errors.count, "error") %>
	</div>
	<ul class="alert">
		<% object.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
		<% end %>
	</ul>
<% end %>